checkCluster <- function(x=x3){
# Ana Fernandes
# makes a check for presence of stratification in SS
for (i in 1:length(x$SSid)){
if (length(b<-x[x$SSclustering!='N',]$SSid)>0){
	print (b)
	stop("ATTENTION: there is clustering for SSid's:")
	}
}
}